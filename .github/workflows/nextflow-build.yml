name: nextflow build
on:   
  workflow_dispatch: 
jobs:
  build:
    runs-on: self-hosted
    steps:
#      - uses: actions/checkout@v3
#      - uses: nf-core/setup-nextflow@v1
      - run: |
         sudo nextflow run epi2me-labs/wf-clone-validation --help
         sudo nextflow run epi2me-labs/wf-clone-validation --primers '/wf-clone-validation/data/primers.tsv' --fastq '/wf-clone-validation/test_data/test/barcode01'
